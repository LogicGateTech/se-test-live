{
  "name": "logicgate-sdet-farm-mock-server",
  "version": "1.0.0",
  "private": true,
  "description": "Small in-memory mock server standing in for the Farm backend -- implements the barn/animal rules so the Angular app has something real to talk to.",
  "main": "src/server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "dev": "ts-node src/server.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  }
}
